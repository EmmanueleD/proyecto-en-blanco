---
import Icon from "../components/Icon.vue";
---

<section class="relative bg-gradient-hero overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 opacity-10">
    <div
      class="absolute inset-0 bg-[url('/images/pattern.svg')] bg-repeat opacity-20"
    >
    </div>
  </div>

  <div class="container relative h-full py-16 md:py-24">
    <div class="grid md:grid-cols-2 gap-12 h-full items-center">
      <!-- Content -->
      <div class="text-white space-y-8 animate-fadeIn">
        <div class="space-y-4">
          <span
            class="inline-block px-4 py-1 rounded-full bg-white/10 backdrop-blur-sm text-secondary font-medium text-sm tracking-wide"
          >
            Terapia de Pareja Especializada
          </span>
          <h1 class="text-4xl md:text-6xl font-display font-bold leading-tight">
            Construye una relación
            <span class="text-secondary">más fuerte</span> y
            <span class="text-secondary">segura</span>
          </h1>
        </div>

        <p class="text-xl md:text-2xl text-gray-100 leading-relaxed">
          Te ayudamos a desarrollar un
          <span class="font-semibold text-secondary">apego seguro</span> en tu
          <span class="font-semibold text-secondary">relación de pareja</span>
          a través de terapia especializada.
        </p>

        <div class="flex flex-col sm:flex-row gap-6 pt-4">
          <button
            onclick="window.location.href='/servicios#calendly-embed'"
            class="btn-secondary transform hover:scale-105 transition-transform duration-300 shadow-lg hover:shadow-xl"
            role="link"
            aria-label="Agenda tu Primera Sesión"
          >
            <span class="flex items-center gap-2">
              Agenda tu Primera Sesión
              <Icon name="fa:arrow-right" client:load className="ml-1" />
            </span>
          </button>
          <button
            onclick="window.location.href='/sobre-nosotros'"
            class="btn-outline border-white text-white hover:bg-white/10 backdrop-blur-sm transition-all duration-300"
            role="link"
            aria-label="Conoce Más"
          >
            Conoce Más
          </button>
        </div>
      </div>

      <!-- Image -->
      <div class="relative order-first md:order-last">
        <div
          class="relative rounded-2xl overflow-hidden shadow-2xl transform hover:scale-[1.02] transition-transform duration-500"
        >
          <div
            class="absolute inset-0 bg-gradient-to-t from-primary/30 to-transparent z-10"
          >
          </div>
          <img
            src="/images/pareja1.jpg"
            alt="Pareja feliz compartiendo un momento especial"
            class="w-full h-[400px] md:h-[600px] object-cover"
            loading="eager"
          />
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes fadeIn {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fadeIn {
    animation: fadeIn 1s ease-out forwards;
  }
</style>
